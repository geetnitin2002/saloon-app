/**
 * @description Entry point for the Home module list.
 */
import view from "./homeComponent";

// module name
export const moduleName = "homeViewComponent";

export default {
  name: moduleName,
  view,
};
