/**
 * @description Entry point for the Home module list.
 */
import view from "./contactComponent";

// module name
export const moduleName = "contactViewComponent";

export default {
  name: moduleName,
  view,
};
